# Automatically generated on 2020-04-25 UTC from https://codemagic.io/app/5ea4910bc8a3e68bd99fd689/settings
# Note that this configuration is not an exact match to UI settings. Review and adjust as necessary.

workflows:
  default-workflow:
    name: Default Workflow
    environment:
      vars:
        SECRETS_JSON: Encrypted(Z0FBQUFBQmVwTGdUSFQ2d1REQ3o4TVJoVW5Vbnd5MzVmMzZwd0dXLVRWYlpPazdLYkNWQS1KdXpnNXRlS3VyRUlOZUtydEJGQnR6bGJXSnVFLTNOc2d6cWpBTktFdndSTmN6WXhYR3l2NzNPM0dwVzVCdldYMTVXRnJRZHZnbFVkUF9kQlJ0MXpCdW9aTnJfVC1oQTdEUDdJRFBDRmptUkp1S0lrOXkwMmlIVnFpTFBQUl9oVmpzPQ==)
      flutter: beta
      xcode: latest
      cocoapods: default
    triggering:
      events:
        - push
        - pull_request
        - tag
      branch_patterns:
        - pattern: '*'
          include: true
          source: true
    scripts:
      - |
        # set up debug keystore
        rm -f ~/.android/debug.keystore
        keytool -genkeypair \
          -alias androiddebugkey \
          -keypass android \
          -keystore ~/.android/debug.keystore \
          -storepass android \
          -dname 'CN=Android Debug,O=Android,C=US' \
          -keyalg 'RSA' \
          -keysize 2048 \
          -validity 10000
      - |
        # set up local properties
        echo "flutter.sdk=$HOME/programs/flutter" > "$FCI_BUILD_DIR/android/local.properties"
      - cd . && flutter packages pub get
      - cd . && flutter config --enable-web
      - |
        # Create directory if it doesn't exist
        mkdir -p $FCI_BUILD_DIR/assets
        # Write out the environment variable as a json file
        echo $SECRETS_JSON | base64 --decode > $FCI_BUILD_DIR/assets/secrets.json
      - cd . && flutter build apk --debug
      - find . -name "Podfile" -execdir pod install \;
      - cd . && flutter build ios --debug --no-codesign
      - |
        # build web
        cd .
        flutter build web --release
        cd build/web
        7z a -r ../web.zip ./*
    artifacts:
      - build/**/outputs/**/*.apk
      - build/**/outputs/**/*.aab
      - build/**/outputs/**/mapping.txt
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - build/web.zip
      - flutter_drive.log
    publishing:
      email:
        recipients:
          - ttarbox0603@gmail.com
      static_pages:
        subdomain: stock_tracker
